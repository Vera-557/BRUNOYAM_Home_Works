package homeWork11.easy;

public class Easy {
    /*
   1. Что такое коллекции в java и для чего они используются?

  Коллекции — это специальные структуры данных в Java, которые созданы для хранения группы объектов одного типа.
  Они нужны для того, чтобы удобно добавлять, удалять, искать и перебирать элементы.
  «контейнеры» для данных, которые заменяют обычные массивы и предлагают больше возможностей для работы.

    2. Назовите 3 основных интерфейса, которые наследуются от интерфейса Collection в java.

   List (список) — хранит элементы в порядке добавления, можно обращаться по индексу;
   Set (множество) — хранит только уникальные элементы, без повторений;

    3. Назовите отличия реализаций ArrayList от LinkedList.

    Главное отличие в том, как они устроены внутри:
    ArrayList основан на обычном массиве. Из-за этого он быстро ищет и получает элементы по индексу,
    но медленно вставляет или удаляет элементы в середине списка.
    LinkedList построен на связном списке — это цепочка отдельных объектов, каждый из которых ссылается на следующий.
    Он быстро вставляет и удаляет элементы в любом месте, но медленно ищет элемент

    4. Что такое множества? Какие есть реализации интерфейса Set в java?

    Множества — это структуры данных, которые хранят только уникальные элементы и не допускают повторений.
    Основные реализации интерфейса Set:
    HashSet — самый быстрый, но порядок элементов не гарантируется;
    TreeSet — хранит элементы в отсортированном порядке;
    LinkedHashSet — сохраняет порядок добавления элементов.

    5. Какая связь между методами equals() и hashCode()? Что такое коллизия?

    hashCode() вычисляет для объекта уникальный номер (число), по которому Java определяет, где он лежит в памяти.
    Когда мы сравниваем два объекта через equals(), Java сначала проверяет их хэш-коды.
    Если они отличаются — объекты точно разные. Если совпадают — тогда сравниваются уже по содержимому через equals().
    Коллизия — это ситуация, когда у двух разных объектов вычисляется одинаковый хэш-код.
    Это случается, потому что хэш-код помещается в число типа int, которое имеет ограниченный размер.

    6. Истинно ли утверждение, что если у двух объектов хеш-код равен, значит и объекты равны?

    нет

    7. Что такое ассоциативный массив в java? Какие реализации интерфейса Map вы знаете?

    Ассоциативный массив (или Map) — это структура данных, которая хранит пары: ключ → значение.
    HashMap — самый быстрый, порядок не важен;
    TreeMap — хранит пары в отсортированном по ключам порядке;
    LinkedHashMap — сохраняет порядок добавления пар;
    Hashtable — устаревшая версия, но потокобезопасная.

    8. Допишите реализацию метода equals() вместо знаков / / для класса Human из 4 урока:

     */
    public static void main(String[] args) {
        Human human1 = new Human("Василий Пупкин", "01.01.1990");
        Human human2 = new Human("Иванов Иван", "01.01.1991");
        System.out.println(human1);
        System.out.println(human1.toString());

    }
}
 class Human {
    private String birthday;
    private String fio;
    public Human(String fio, String birthday) {
        this.fio = fio;
        this.birthday = birthday;
    }
     @Override
     public String toString() {
         return "FIO " + fio + " Birthday " + birthday;
     }
     @Override
     public boolean equals(Object obj) {
         if (obj == null || getClass() != obj.getClass()) return false;
         Human humanObj = (Human) obj;
         boolean result = fio.equals(humanObj.fio)
                 && birthday.equals(humanObj.birthday);
         return result;
     }
}
